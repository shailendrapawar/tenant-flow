export const ENV = {
  APP_NAME: import.meta.env.VITE_APP_NAME || "Tenant Flow",
  APP_ENVIRONMENT: import.meta.env.VITE_APP_ENVIRONMENT || "development",

  API_URL: import.meta.env.VITE_API_URL || "http://localhost:3000",
}
